{
  "resourceType": "StructureDefinition",
  "id": "contained-invariant-profile",
  "url": "http://hl7.org//fhir/test/StructureDefinition/contained-invariant-profile",
  "version": "1.0.0",
  "name": "MyContainedProfile",
  "status": "draft",
  "fhirVersion": "4.0.1",
  "kind": "resource",
  "abstract": false,
  "type": "Patient",
  "baseDefinition": "http://hl7.org/fhir/StructureDefinition/Patient",
  "derivation": "constraint",
  "differential": {
    "element": [
      {
        "id": "Patient.contained",
        "path": "Patient.contained",
        "type" : [{
          "code" : "Practitioner"
        }],
        "constraint": [
          {
            "key": "cont-1",
            "severity": "error",
            "human": "Check context variables are set correctly",
            "expression": "%context.type().name.trace('cn') = 'Practitioner' and %resource.type().name.trace('rn') = 'Patient' and %rootResource.type().name.trace('rrn') = 'Patient'"
          }
        ]
      },
      {
        "id": "Patient.contained.name",
        "path": "Patient.contained.name",
        "constraint": [
          {
            "key": "cont-2",
            "severity": "error",
            "human": "Check context variables are set correctly",
            "expression": "%context.type().name.trace('cn') = 'HumanName' and %resource.type().name.trace('rn') = 'Practitioner' and %rootResource.type().name.trace('rrn') = 'Patient'"
          }
        ]
      },
      {
        "id": "Patient.generalPractitioner",
        "path": "Patient.generalPractitioner",
        "constraint": [
          {
            "key": "cont-3",
            "severity": "error",
            "human": "Check context variables are set correctly",
            "expression": "%context.type().name.trace('cn') = 'Reference' and %resource.type().name.trace('rn') = 'Patient' and %rootResource.type().name.trace('rrn') = 'Patient'"
          }
        ]
      }
    ]
  }
}