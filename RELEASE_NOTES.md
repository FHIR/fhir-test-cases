* Update tests for fixes to SDC rendering
* Add additional logical model snapshot and validation tests + more CDS hooks tests
* Add new test for maxValue on decimal
* Changes to validator error messages
