* Align tx expansion test cases with tx-ecosystem tests
* Add tests for compliesWith extension
* Update date/duration validation test cases now that implementation is in place
* Add cross-version validation test case (ex-vsac value set)
* Fix Background colour in comparison (WCAG fix)

